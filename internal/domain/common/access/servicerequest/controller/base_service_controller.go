package controller

// BaseServiceController maps to BaseServiceController.java
// In Go, since we don't have abstract classes, this serves as an embeddable struct or interface if needed.
// @MappedFrom BaseServiceController
type BaseServiceController struct {
}
